/**
 * components/index/partials/summary-content.js
 *
 * The summary content partials is merely a presentational component that renders the text
 * for the summary content. It's simple, static content and does not need any special actions
 * or properties in this related .js file
 */
import Ember from 'ember';

export default Ember.Component.extend({
});
