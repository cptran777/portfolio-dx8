import Ember from 'ember';

export default Ember.Component.extend({
  /**
   * Class names to assign to the rendered element for this component
   * @type {Array}
   */
  classNames: ['col-xs-6', 'col-sm-6', 'col-md-3']
});
