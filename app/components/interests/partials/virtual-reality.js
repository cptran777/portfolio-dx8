/**
 * components/interests/partials/virtual-reality.js
 *
 * THis component is a simple partial component meant to render the information for the virtual
 * reality interest card detail section. It only takes one property, and that is a flag to 
 * determine whether to render expanded content or collapsed content.
 */
import Ember from 'ember';

export default Ember.Component.extend({
});
