import formatting from 'portfolio-dx8/utils/formatting';
import { module, test } from 'qunit';

module('Unit | Utility | formatting');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatting();
  assert.ok(result);
});
