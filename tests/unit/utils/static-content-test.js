import staticContent from 'charlie-tran/utils/static-content';
import { module, test } from 'qunit';

module('Unit | Utility | static content');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = staticContent();
  assert.ok(result);
});
